import { types } from "./types";//type of data
import { mutations } from "./mutations";
import { resolvers } from "./resolvers";
import { queries } from "./queries";


export const RatingAndReviews = {types,mutations,resolvers,queries}