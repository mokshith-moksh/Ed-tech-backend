import { types } from "./types";//type of data
import { queries } from "./query";//type of data
import {mutations} from "./mutations"
import {resolvers} from "./resolvers"


export const User = {types,queries,mutations,resolvers}